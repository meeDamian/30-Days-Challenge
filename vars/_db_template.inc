<?
    /**
     * STEPS TO FOLLOW ON THIS FILE:
     * 1. fill all of the required data below
     * 2. change $db['on'] to true
     * 3. rename this file to _db_.inc
     * 4. ?????????????
     * 5. PROFIT!
     */

    $db = Array();
    $db['on'] = false;

    if($db['on']) {
        $db['host'] = "";
        $db['name'] = "";
        $db['user'] = "";
        $db['pass'] = "";
    }
?>
